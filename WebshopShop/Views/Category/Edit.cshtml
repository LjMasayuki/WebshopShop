@model WebshopShop.ViewModels.Admin.CategoryViewModel

@{
ViewData["Title"] = "Edit Category";
}

<div class="adm-header">
    <div class="adm-header-left">
        <p class="adm-breadcrumb"><a asp-controller="Category" asp-action="Index">Categories</a> / Edit</p>
        <h1 class="adm-title">Edit Category</h1>
        <p class="adm-subtitle">Rename this webshop category.</p>
    </div>
</div>

<form asp-controller="Category" asp-action="Edit" method="post" class="adm-form adm-form-narrow">
    @Html.AntiForgeryToken()
    <input asp-for="Id" type="hidden" />

    <div class="adm-form-group">
        <label asp-for="Name" class="adm-form-label">Category Name</label>
        <input asp-for="Name" class="adm-form-input" />
        <span asp-validation-for="Name" class="adm-form-error"></span>
    </div>

    <div class="adm-form-actions">
        <button type="submit" class="adm-btn adm-btn-primary">Save Changes</button>
        <a asp-controller="Category" asp-action="Index" class="adm-btn adm-btn-ghost">Cancel</a>
    </div>
</form>